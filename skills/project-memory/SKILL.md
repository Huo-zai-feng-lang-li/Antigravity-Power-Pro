---
name: project-memory
description: 项目核心索引。只记干货，不记流水账。每次更新本 Skill 时，必须删除已过时的旧版本细节。避免“记忆膨胀” 和 “记录无效性” 的风险。Skill 不应该记录“过去所有的事”，而应该记录 “为了继续开发，我现在必须知道的事”。
---

# 🛠️ 项目干货索引 (v2.5.7)

## 📍 现状 (Current)

- **版本**: `v2.5.7` 稳定版。


## ⚠️ 技术约束 (Technical Constraints)

- **DOM 注入**: 必须精确定位 `.shrink-0.flex.items-center.gap-1`。
- **逻辑同步**: 严禁修改 Panel 副本。所有 Panel 逻辑必须在 `shared/` 修改，由 Rust 后端执行分发。
- **视觉规范**: 按钮严守 **圆角矩形** 规范。

## 📋 流水线 (Pipeline)

- **推送**: 强制代理 `127.0.0.1:51081`。
- **版本控制**: 变更必须同步覆盖 `package.json`, `Cargo.toml`, `README.md`。
- **治理原则**: 每次任务结束必须依据“修剪原则”重写本文件，剔除过期细节。
