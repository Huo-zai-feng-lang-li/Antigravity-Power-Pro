#!/usr/bin/env sh
# 作用：在 git commit 之前执行
# 功能：
# 1. 显示提示信息
# 2. 执行 npx lint-staged（ESLint + Prettier 格式化）
# 何时触发：每次 git commit 时
. "$(dirname -- "$0")/_/husky.sh"

echo "🤖 Tips: 请确保控制台无错误、及警告信息,且页面自适应UI无误后,方可提交代码！"

echo "🛠️ 自动开启代码运行扫描检查中 ~ ♻️"

# 提交前检查所有
# npm run CheckAll

# 只对提交的代码进行 eslint 检查和 prettier 格式化 
npx lint-staged